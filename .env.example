# Copiá este archivo a ".env" y completá tu clave.
# Sin APP_PASSWORD, la app queda sin login (accesible sin contraseña).
APP_PASSWORD=

# Opcional: puerto donde corre la app (por defecto 5050)
PORT=5050
